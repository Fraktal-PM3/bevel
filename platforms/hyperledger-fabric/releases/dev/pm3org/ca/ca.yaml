apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: ca
  namespace: pm3org-net
  annotations:
    fluxcd.io/automated: "false"
spec:
  interval: 1m
  releaseName: ca
  chart:
    spec:
      interval: 1m
      sourceRef:
        kind: GitRepository
        name: flux-dev
        namespace: flux-dev
      chart: platforms/hyperledger-fabric/charts/fabric-ca-server   
  values:
    global:
      serviceAccountName: vault-auth
      cluster:
        provider: minikube
        cloudNativeServices: false
        kubernetesUrl: https://46.62.234.169:18443
      vault:
        type: hashicorp
        network: fabric
        address: http://46.62.234.169:8200
        authPath: devpm3org
        secretEngine: secretsv2
        secretPrefix: "data/devpm3org"
        role: vault-role
        tls: false
      proxy:
        provider: haproxy
        externalUrlSuffix: org1proxy.pm3fraktal.se

    storage:
      enabled: false
      nameOverride: pm3org-bevel-storageclass
      size: 512Mi

    image:
      alpineUtils: ghcr.io/hyperledger/bevel-alpine:latest
      ca: ghcr.io/hyperledger/bevel-fabric-ca:latest
      pullSecret: regcred

    server:
      removeCertsOnDelete: true
      tlsStatus: true
      adminUsername: pm3org-admin
      adminPassword: pm3org-adminpw
      subject: "/C=SE/ST=Lulea/L=Lulea/O=Orderer"
      clusterIpPort: 7054

    labels:
      service:
        - example1: example2
      pvc:
      deployment:
